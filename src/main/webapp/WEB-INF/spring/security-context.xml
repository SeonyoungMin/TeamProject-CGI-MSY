<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
                 http://www.springframework.org/schema/beans
                 http://www.springframework.org/schema/beans/spring-beans.xsd
                 http://www.springframework.org/schema/security
                 http://www.springframework.org/schema/security/spring-security.xsd">
                 
    <beans:bean name="mvcHandlerMappingIntrospector" class="org.springframework.web.servlet.handler.HandlerMappingIntrospector"/>

    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <http use-expressions="true">

        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')"/>

        <intercept-url pattern="/"                access="permitAll"/>
        <intercept-url pattern="/index.jsp"       access="permitAll"/>
        <intercept-url pattern="/member/login"    access="permitAll"/>
        <intercept-url pattern="/member/login/**"     access="permitAll"/>
        <intercept-url pattern="/product/**"      access="permitAll"/>
        <intercept-url pattern="/board/**"        access="permitAll"/>
        <intercept-url pattern="/resources/**"    access="permitAll"/>
        <intercept-url pattern="/WEB-INF/views/error/**" access="permitAll"/>

        <intercept-url pattern="/favorite/**"      access="isAuthenticated()"/>
        <intercept-url pattern="/comment/write"    access="isAuthenticated()"/>
        <intercept-url pattern="/comment/edit"     access="isAuthenticated()"/>
        <intercept-url pattern="/comment/delete"   access="isAuthenticated()"/>
        <intercept-url pattern="/trade/**"         access="isAuthenticated()"/>

        <intercept-url pattern="/**" access="isAuthenticated()"/>

        <form-login login-page="/member/login"
                    login-processing-url="/member/loginProcess"
                    username-parameter="username"
                    password-parameter="password"
                    default-target-url="/"
                    authentication-failure-url="/member/login?error=true"
                    always-use-default-target="false"/>

        <logout logout-url="/member/logout" logout-success-url="/"/>

        <csrf/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <password-encoder ref="passwordEncoder"/>
            <jdbc-user-service data-source-ref="dataSource"
            				   users-by-username-query="SELECT id, password, 1 FROM employee3 WHERE id = ?"
            				   authorities-by-username-query="SELECT id, role FROM employee3 WHERE id = ?"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
